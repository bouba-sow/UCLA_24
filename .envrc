# Auto-loaded by direnv when entering the UCLA_24 project directory.
# On systems where $SCRATCH is not set (habilis/oberon), jz_cache_env.sh
# silently no-ops, so this file is safe to commit and sync everywhere.
source "code/scripts/slurm/jz/jz_cache_env.sh"
